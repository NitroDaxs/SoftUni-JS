//This function prints the characters of a given string on the console.
function stringToChars(input) {
    for (let i = 0; i < input.length; i++) {
        console.log(input[i]);
    }
}

stringToChars('Hello, JavaScript!');